#include "order_book.hpp"

// Placeholder translation unit. All OrderBook methods are defined inline in
// the header for maximum inlining on the hot path; this file exists so the
// build has a stable home for any future non-inline implementation.
