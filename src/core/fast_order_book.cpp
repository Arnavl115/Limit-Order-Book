#include "fast_order_book.hpp"

// Placeholder translation unit. All FastOrderBook methods are defined inline
// in the header for maximum inlining; this file exists so the build has a
// stable home for any future non-inline implementation.
